# Roadmap Be Stronger

**Última auditoría completa: 2026-09-16.** Este documento se construye contrastando código real contra `docs/PENDIENTE_BACKEND_ADMIN.md`, `docs/TODO_INVENTORY.md` y `docs/DEAD_SCREENS.md` — los tres están actualizados a la misma fecha. Si algo de aquí no coincide con esos documentos, esos tres mandan (tienen el detalle línea a línea); este documento es el resumen priorizado para planificar, no la fuente de verdad de cada endpoint.

**Regla para no repetir la desactualización que motivó esta pasada**: cuando se resuelva un item de aquí, márcalo `~~tachado~~ ✅ RESUELTO (fecha)` en este archivo **y** en el documento de origen (`PENDIENTE_BACKEND_ADMIN.md`/`TODO_INVENTORY.md`/`DEAD_SCREENS.md`) en el mismo commit — no en una sesión futura "cuando se acuerde alguien".

---

## Ya resuelto (auditoría 2026-09-16) — sin acción, solo referencia histórica

Todo esto apareció alguna vez como "pendiente" en algún documento de este repo y ya está confirmado hecho, leyendo el código real de `bsa`/`Bckbs`/`bstronger-admin` (no solo nombres de ruta):

- Onboarding v2 (3 cuestionarios + completado + admin panel)
- Reorganizar semana en el calendario
- "Solicitar función"/"Informar de error" (+ panel admin con filtros)
- Borrado de cuenta
- Workout demo auto-asignado a usuarios nuevos
- Imagen por recurso (`resources.image_url`)
- Subida real de foto de perfil
- Cierre de sesión de entrenamiento (duración/calorías/logros)
- Recordatorios locales (resuelto 100% client-side, sin backend)
- OTP (eliminado)
- Pantallas de vídeo (eliminadas)
- Moderación de publicaciones (persistencia del reporte + panel admin + permiso de borrado ampliado)
- Motor de Auto-Regulación de Carga — **las 9 piezas** de integración en el admin panel (sustituciones de ejercicio, feed de logros, readiness admin, reglas de progresión, sugerencias pendientes, planes semanales adaptativos, excepciones de coach, override de experiencia, toggle de semana de descarga)
- Seguridad backend: saneado de 5xx, rate limiting en auth, IDOR en posts/hábitos/métricas, logout en todos los dispositivos
- Checkout Stripe/PayPal — **eliminado** por completo (decisión de negocio: pago 100% presencial)
- Reporte de comentarios + bloqueo de usuario — `COMMUNITY_ENABLED = true`
- Chat/FitBot — **decisión tomada** de dejarlo oculto tras el flag tal cual (es un bot IA privado 1-a-1, sin coach ni otros usuarios de por medio; sin plan de completar la integración de OpenAI por ahora) — cerrado, no es "pendiente"
- Las 14 PRs de Dependabot que estaban abiertas — todas mergeadas a `master` (2026-09-16)
- `MigratedRecipeTagList` ya lee `recipe_tags.group` cuando existe, con fallback a la heurística de texto solo para tags sin categorizar (2026-09-16)
- Los 2 TODOs reales de `about_app_screen.tsx` — resueltos borrando el bucle muerto (`aboutPages`/`loadAppSettings()` nunca se completaron; los 4 botones reales de la pantalla son independientes de eso) (2026-09-16)
- `GET readiness-scores-latest` — `combined_score`/`band`/`acwr` reales para el hero de Home (Recovery prioriza el dato real, Strain ya no es placeholder fijo cuando hay `acwr`) — código en `bsa` + `Bckbs` [PR #18](https://github.com/ilzarpeatore/Bckbs/pull/18), **mergeada** a `main` (2026-09-16)
- PR #25 de `bsa` mergeada a `master` (2026-09-16) — incluye todo lo de este documento hasta ese punto (fixes de Android, limpieza de docs, moderación de comunidad, `recipe_tags.group`, `readiness-scores-latest` en cliente)
- Auditoría de alcanzabilidad de `DEAD_SCREENS.md` rehecha por completo — de 190 pantallas registradas (04-08-2026, ya no correcto) a 87 reales hoy, de las que solo 5 estaban muertas de verdad (documento reescrito entero) (2026-09-16)
- Las 5 pantallas muertas encontradas en esa auditoría — `ExerciseInfo` (registro duplicado), `MigratedChewie`, `MigratedTermsAndConditions`, `MigratedTips`, `MigratedViewEquipment` — **borradas** (registro en `App.tsx`/`ScreenExplorer.tsx` + los 4 archivos de componente; `MigratedExerciseInfo` sigue vivo). Usuario confirmó explícitamente borrar las 3 sin motivo conocido en vez de retomarlas. `npx tsc --noEmit` limpio tras el borrado (2026-09-16)
- **`MigratedViewEquipment` recuperada la misma noche** — era la única de las 3 "sin motivo conocido" ya funcional (datos reales de `exercisesApi.getEquipment()`), a diferencia de `MigratedChewie` (reproductor roto, `expo-av` ya no existe) y `MigratedTips` (probable duplicado de una pestaña de `MigratedExerciseInfo`). Restaurada desde el commit previo al borrado; sigue sin punto de entrada real, ver item 20 abajo (2026-09-16)
- **Rondas 7-16 del Motor de Auto-Regulación (ítems 23-45) verificadas contra BD real** (2026-09-16 noche, VPS `testapp.bestronger.es`, todo dentro de transacciones con rollback) — 24 aserciones en verde: nivel de experiencia, sustitución inteligente, redondeo por `increment_kg`, `rol_ejercicio`, `dolor_reciente_no_bloqueante`, síntesis N-de-M (`min_condiciones_requeridas`), semana de descarga (`is_deload`/`markWeekDeload`), y feed de logros ampliado (umbral escalado por experiencia, `MEJOR_MARCA_RECIENTE`, `MANTIENE_FUERZA_EN_DEFICIT`). Detalle completo en `docs/PENDIENTE_BACKEND_ADMIN.md`. Hallazgo colateral: 1 bug crítico real encontrado, corregido, fusionado y desplegado el mismo día (item `0e` de la sección de pendientes, marcado resuelto ahí)
- **Push Key APNs (iOS) creada y asignada al proyecto EAS** (2026-09-16 noche) — Key ID `83J67GTP7S`, Apple Team `6T7NPV7M2J`, asignada al target `bestronger` (`com.pfndesign.bestronger`) vía `eas credentials`. En el camino se corrigieron 2 bugs de config reales que bloqueaban el comando: `app.json` tenía `"slug": "bestronger"` pero el proyecto EAS está registrado como `"be-stronger"` (corregido); no existía `eas.json` (creado, mínimo, solo perfil `production`, no toca el pipeline de build real que sigue siendo el custom de GitHub Actions). El target `bestrongerWidgetsExtension` se dejó sin configurar a propósito (push de Live Activities es un caso aparte, no pedido). Queda pendiente el lado Android (Firebase/FCM, ver 0a-bis)
- **Panel de Tareas del admin** (2026-09-16/17 noche, pedido explícito) — `/tasks` en `bstronger-admin` con pestañas Gestión (a mano: categoría, prioridad, fecha límite, cliente) y Desarrollo (sincronizada desde esta sección del roadmap). Reutiliza la tabla `tasks`/`TaskController` ya existente desde 2026-07-21 en vez de duplicar ([`Bckbs` PR #20](https://github.com/ilzarpeatore/Bckbs/pull/20), [`bstronger-admin` PR #18](https://github.com/ilzarpeatore/bstronger-admin/pull/18), ambas mergeadas y desplegadas). Convención de sincronización documentada en `CLAUDE.md` — ver esa nota antes de editar la sección "Pendiente real, priorizado" de aquí en adelante, hay que llamar también a `POST admin/task-sync`. **Bug real encontrado en el primer uso real** (`$request` indefinido dentro del closure de la transacción, 500 en toda llamada) — corregido, mergeado y desplegado el mismo día ([`Bckbs` PR #22](https://github.com/ilzarpeatore/Bckbs/pull/22)). Primera sincronización real ejecutada: los 19 items de la sección de abajo ya están cargados en el panel

---

## Pendiente real, priorizado

### App (`bsa`) — código nuevo pendiente, no bloqueado externamente

| #   | Qué                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Bloqueado por           |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| 0d  | **✅ RESUELTO por completo (2026-09-16 noche).** Integrados en el onboarding/ajustes los 3 campos nuevos + 1 endpoint nuevo del backend (commit `6c8dd0d`): PAR-Q+ gana `parq_pregnant_or_possible`/`parq_menstrual_change_or_stress_fracture` (mostradas/enviadas solo si `gender==='female'`, vía `showIf` nuevo en `types/onboardingV2.ts`) + `parq_eating_disorder_history` (siempre); nutrición gana 3 campos de disponibilidad de cocina; pantalla nueva "Disponibilidad de entrenamiento" (menú Perfil > Preferencias). [`Bckbs` PR #19](https://github.com/ilzarpeatore/Bckbs/pull/19) mergeada y **desplegada a la VPS** (`git pull` + `composer install` + 5 migraciones aplicadas, verificado con 200/401 reales) el mismo día | Nada — ya en producción |
| 0e  | ✅ **RESUELTO Y DESPLEGADO (2026-09-16 noche).** Bug crítico encontrado verificando el Motor de Auto-Regulación: `ClientExerciseLogObserver::maybeRecordPrReps()` generaba SQL inválido para MySQL (`LIMIT` dentro de un subquery `IN`, error 1235) — rompía con 500 todo guardado de serie de cualquier cliente de pago. [`Bckbs` PR #21](https://github.com/ilzarpeatore/Bckbs/pull/21), fusionado con confirmación explícita del usuario (commit `9fba012`) y desplegado a la VPS. Verificado dos veces contra BD real, incluida una prueba end-to-end disparando el observer real ya desplegado. Detalle en `docs/PENDIENTE_BACKEND_ADMIN.md`                                                                                         | Nada — ya en producción |

### Build / despliegue — te toca a ti, no es código nuevo

| #      | Qué                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Bloqueado por                                                                                                                    |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| 0a     | **✅ RESUELTO (2026-09-16 noche).** Relanzar el build de IPA ya no tiene bloqueantes de Apple: (1) Push Notifications habilitado en el App ID `com.pfndesign.bestronger`; (2) perfil "Be Stronger App Store" regenerado por Apple automáticamente al activar la capability (`Status: Active`, `Enabled Capabilities: In-App Purchase, Push Notifications`, confirmado por el usuario), descargado y el secret `IOS_PROVISIONING_PROFILE` en GitHub actualizado por Claude vía `gh secret set` (verificado con `openssl smime`: el perfil contiene `aps-environment: production`, coincide con el entitlement). Queda solo **lanzar el `workflow_dispatch` de `ios-build.yml`** (`ios_path: "ios"`, `configuration: "Release"`)                 | Nada — listo para lanzar                                                                                                         |
| 0a-bis | **iOS: ✅ RESUELTO (2026-09-16 noche)** — Push Key APNs creada y asignada al proyecto EAS (Key ID `83J67GTP7S`, Apple Team `6T7NPV7M2J`, target `bestronger`), ver "Ya resuelto" arriba. **Android: sigue pendiente.** El backend manda notificaciones vía la API de Expo (`app/Notifications/Channels/ExpoPushChannel.php` → `https://exp.host/--/api/v2/push/send`), no habla con APNs/FCM directamente. `app.json` tiene `extra.eas.projectId` (`e4a24c99-7e50-462c-a430-e2843f03846d`). **Android: ni siquiera existe un proyecto Firebase** — no hay `google-services.json` en el repo ni `googleServicesFile` en `app.json`, hace falta crear el proyecto Firebase primero y luego subir sus credenciales (FCM V1 service account) a EAS | Android: acceso a Firebase Console (crear proyecto) + login de Expo/EAS — no ejecutable por Claude sin que el usuario haga login |
| 0b     | Publicar la ficha en Play Console + subir el primer AAB firmado — el pipeline ya está verificado (run `34273584804`, `docs/BUILD_AAB.md`), falta el paso humano de crear la ficha en Play Console y subir el artefacto                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Acceso a Play Console (no verificable desde el repo si ya se hizo)                                                               |
| 0c     | **✅ RESUELTO (2026-09-16/17 noche).** Desplegado `Bckbs`/`bstronger-admin` al VPS — `git pull` a `main` HEAD en ambos (Bckbs hasta `752c821`, incluye Panel de Tareas + readiness-scores-latest + fix de PR crítico; admin hasta `07851cb`) + `php artisan migrate --force` + `npm run build` del admin panel (servido estático por Caddy desde `/var/www/testapp/admin/dist`, no Netlify — resuelve esa duda antigua). Corregidos permisos del build (quedaba `root:root` al compilar por SSH como root, devuelto a `www-data:www-data`). Smoke test `200 OK`                                                                                                                                                                                | Nada — ya en producción                                                                                                          |

### Datos que solo tú puedes dar (sin código real que escribir)

| #   | Qué                                       | Bloqueado por                                  |
| --- | ----------------------------------------- | ---------------------------------------------- |
| 4   | `APP_STORE_ID` en `constants/appLinks.ts` | Publicar la ficha en App Store Connect primero |
| 5   | `PLAY_STORE_PUBLISHED = true`             | Publicar la ficha en Play Console primero      |
| 6   | `SOCIAL_LINKS` en `constants/appLinks.ts` | Cuentas reales de redes sociales, si existen   |

### Contenido — tarea de panel admin, no de código

| #   | Qué                                                                                                                                           |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| 7   | Marcar manualmente qué `WorkoutTemplate`/`Recipe` son `is_exclusive`/`is_premium` (el flag ya funciona)                                       |
| 8   | Catálogo de `Diets` real (hoy solo 1 fila de prueba)                                                                                          |
| 9   | Fotos reales de las 5276 recetas (100% placeholder hoy); antes decidir subida manual vs. integración por lote y revisar licencias de imágenes |
| 10  | Bibliografía real de los 4 posts del blog (el acordeón ya existe en la app)                                                                   |

### Infraestructura / seguridad operativa — fuera de este repo, requiere acceso al VPS

| #   | Qué                                                                                                                                                 |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| 11  | Auditar claves SSH autorizadas en el VPS (`/root/.ssh/authorized_keys`), quitar las obsoletas                                                       |
| 12  | Confirmar que la contraseña de la BD de producción es robusta                                                                                       |
| 13  | Rotar la contraseña de la cuenta `demo@bestronger.app` (expuesta en el incidente de recuperación de datos del 2026-08-05, si sigue siendo la misma) |
| 14  | Revisar si algún otro usuario en producción tiene contraseña por defecto/predecible                                                                 |

### Auditoría de datos (no bloqueante)

| #   | Qué                                                                                                                                                     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 15  | Confirmar si algún hábito personalizado creado antes del 2026-08-05 se perdió en el incidente `migrate:fresh` de esa fecha, y recrearlo a mano si falta |

### Diferido explícitamente (sin fecha, según demanda real de negocio)

| #   | Qué                                                                                                                                                                                                                           | Complejidad                                                 |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| 16  | Wearables (Garmin/Fitbit/Apple Watch/Galaxy Watch) — sin backend propio                                                                                                                                                       | XL                                                          |
| 17  | Integración Strava (OAuth2 + webhooks + `strava_connections`)                                                                                                                                                                 | L                                                           |
| 18  | HealthKit/Health Connect — **ya no es "reactivar", es construir desde cero**: el código (`helper/health.ts`, entitlements, pantallas de emparejamiento) se eliminó por completo el 2026-09-10, no solo se ocultó tras un flag | L, condicionado a Apple Developer Program de pago ($99/año) |
| 19  | Web (`webbs`): catálogo `/programas` + checkout — solo si el modelo de negocio pasa a venta online (hoy es 100% presencial)                                                                                                   | L                                                           |

### Deuda técnica

| #   | Qué                                                                                                                                                                                                                                                                                                                                                | Complejidad |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| 20  | Dar un punto de entrada real a `MigratedViewEquipment` (recuperada 2026-09-16) — candidato natural: filtro "Por equipamiento" en `search_screen.tsx`, mismo patrón que el filtro por zona del cuerpo que ya navega a `MigratedViewBodyPart`. Sin esto vuelve a ser "muerta" en el sentido estricto de `DEAD_SCREENS.md`, aunque el código funcione | S           |

### Bugs reportados por verificar (2026-09-17, sin confirmar aún)

| #   | Qué                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Bloqueado por                                             |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| 21  | **✅ RESUELTO Y DESPLEGADO (2026-09-18 noche).** `SessionDetailController::addExercise/addBlock/removeExercise` reescritos sobre un overlay por cliente (`client_exercise_overrides`/`client_block_overrides`) en vez de mutar la plantilla (`WorkoutTemplate`) compartida — con `assertClientOwnsAssignment()` como guard de propiedad. `assignDirect()` clona la plantilla al asignar (`WorkoutTemplate::cloneStructure()`). Móvil (`ClientCalendarController::getDayDetail()` + `workout_session_screen.tsx`, commit `bsa` `bc65f96`) y admin (`WorkoutTemplateViewer`, commits `Bckbs` `2822b69`/`bstronger-admin` `1234415`) ya renderizan las adiciones/ocultaciones propias de cada cliente. Verificado end-to-end contra BD real (2 clientes compartiendo `ProgramDayAssignment`, rollback tras la prueba): cliente A añade/oculta y cliente B no ve el cambio; cliente sin asignación real bloqueado con 403. Detalle completo, gap conocido de bajo impacto (`client_block_overrides` sin columna `hidden`) y todos los commits en `docs/AUDITORIA_ENDPOINTS_2026-09-18.md`. **Pendiente real que queda**: los cambios en `bsa` son JS embebido en el bundle nativo — no llegan por OTA, hace falta lanzar un build nuevo (IPA/APK) para que el cliente los vea | Build nuevo (IPA/APK) para que llegue al dispositivo real |
| 22  | Reportado por el usuario: fallos en los botones y en los diálogos de asignación de workouts (a confirmar si ocurre en la app, en el admin panel, o ambos, y qué acción concreta falla)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Nada — pendiente de investigar                            |
| 23  | **✅ RESUELTO (2026-09-18 noche, commit `9baf70c`).** El script inyectado en el WebView de `resource_detail_screen.tsx`/`blog_detail_screen.tsx` solo medía la altura del documento y avisaba a React Native una vez, en `onload` — con los acordeones `<details>` todavía colapsados. Al expandir uno, el contenedor WebView se quedaba con la altura del estado colapsado. Fix: escucha el evento nativo `toggle` en fase de captura sobre `document` y vuelve a medir/avisar tras un `requestAnimationFrame`. Pendiente de build nuevo para que se vea en la app instalada real (mismo motivo que el item 21)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Build nuevo (IPA/APK) para que llegue al dispositivo real |
| 24  | Los recursos (`resources_list_screen.tsx` y `resource_detail_screen.tsx`) no muestran `image_url` en ningún sitio, a diferencia del blog (`blog_screen.tsx`/`view_all_blog_screen.tsx` sí renderizan `post_image`). Los 6 recursos ya tienen portada individual en BD (asignada 2026-09-17) pero no se ve en la app — falta añadir el `<Image>` en ambas pantallas                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Nada — listo para implementar                             |
| 25  | **Parcial (app ✅ 2026-09-18, admin ❌ pendiente).** Pantalla nueva `onboarding_data_screen.tsx` ("Mis respuestas del onboarding", menú Perfil > Cuenta) ya permite editar PAR-Q+/salud, cuestionario de entrenamiento y de nutrición completos, con botón "Guardar" por sección. La mitad admin sigue sin hacer: `bstronger-admin/UserDetailView.tsx` (sección Onboarding) solo muestra estos datos en badges/texto de solo lectura, sin ningún input ni botón de guardar                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Admin: nada, listo para implementar                       |

---

## Cómo se llegó a esta lista

Este roadmap nació de una sesión anterior que asumió como "pendiente" todo lo que decían los documentos internos sin verificar contra el código — varios items resultaron ya estar hechos (a veces desde hace semanas). La lección aplicada aquí y en `PENDIENTE_BACKEND_ADMIN.md`/`TODO_INVENTORY.md`/`DEAD_SCREENS.md`: cada item de este documento se verificó leyendo el archivo/endpoint real citado, no solo el nombre de la ruta o lo que decía un documento anterior. Cuando un item de aquí se resuelva, actualizar ambos sitios en el mismo commit — es la única forma de que esto no vuelva a desincronizarse.
